#pragma once

enum class MenuOptions {
    BACK,
    EXIT,
    PLAY,
    CONTROLS,

    // set controls, keys
    SET_KEY_ATTACK,
    SET_KEY_JUMP,
    SET_KEY_LEFT,
    SET_KEY_RIGHT,
    SET_KEY_UP,
    SET_KEY_DOWN,

    // set controls, Joystick
    SET_JOYSTICK_ATTACK,
    SET_JOYSTICK_JUMP
};